# 引き継ぎメモ ─ Bottle Scanner ショートドラマ動画制作

> このファイルは「別リポジトリ・新しいチャット」で作業を続けるための引き継ぎデータです。
> `bottle-scanner-video/` フォルダごと新しいリポジトリに置けば、そのまま制作を再開できます。

---

## 0. これは何か

Bar向けアプリ「**Bottle Scanner**」のプロモ用ショートドラマ（35秒）の**制作素材・指示書・管理システム一式**。
最終的な映像・音声の合成と納品は制作者が **Final Cut Pro X（FCPX）** で行う前提。
その手前までの「素材と設計図」がこのパッケージ。

制作者ワークフロー：**DomoAI（ブラウザ／映像生成）→ Irodori（ローカル／音声生成）→ FCPX（合成・納品）**

---

## 1. 確定済みの決定事項（新チャットでもこの前提を維持）

| 項目 | 確定値 |
|------|--------|
| アスペクト比 | **9:16 縦型**（Instagram Reels / TikTok / YouTube Shorts） |
| 解像度 | 1080 × 1920 px |
| フレームレート | 30fps |
| 尺 | 35秒（5シーン・10ショット） |
| ビジュアル | **実写シネマティック（photoreal）**。高級Barの実写ドラマ調 |
| 音声言語 | 日本語（Irodoriで生成） |
| ブランド色 | アンバー `#E8B451`（おもてなし）× ミント `#3FE0C5`（スキャナのテック）／背景チャコール `#14110E` |
| CTA | 「詳しくはプロフィールから」 |

---

## 2. 台本（35秒 / 5シーン）要約

1. **シーン1（0:00–0:08）**：新人ケンジが常連マナミの「山崎」注文を受け、ボトルキープの壁で固まる
2. **シーン2（0:08–0:18）**：ラベルを1本ずつ探す焦り。マナミが時計を見る／店長タカシが心配「大丈夫か？」
3. **シーン3（0:18–0:24）**：ひらめいてスマホでBottle Scanner起動 → **ARで一発発見「…あった！」**（暗→明の反転＝気持ちよさの山）
4. **シーン4（0:24–0:32）**：迷わず提供。マナミ「早いね。頼りになる」／タカシ満足げにうなずく（オチ）
5. **シーン5（0:32–0:35）**：CTAエンドカード「探す時間を、おもてなしの時間に。」

**登場人物**：ケンジ（新人・20代男）／マナミ（常連・30代女）／タカシ（店長・40代後半男）
→ 3人の**固定外見記述子（キャラトークン）**は `bottle-scanner-video/01_production-bible.md` に定義済み（一貫性の要）。

---

## 3. パッケージ構成（`bottle-scanner-video/`）

| ファイル | 役割 |
|---------|------|
| `README.md` | 全体ワークフロー・命名規則の司令塔 |
| `01_production-bible.md` | 企画・キャラトークン・ライティング/カラー指針・共通スタイルサフィックス |
| `02_shot-list.md` | 10ショットの絵コンテ表（タイムコード付き） |
| `03_domoai-prompts.md` | 各ショット keyframe画像＋I2V＋T2V プロンプト（英語・共通ネガティブ） |
| `04_irodori-voice-script.md` | 全セリフの読み・感情・速度・命名規則 |
| `05_se-bgm-cue-sheet.md` | SE/BGM検索ワード・ヒーロー通知音「ピロン」スペック |
| `06_graphics-telop-spec.md` | 字幕・CTAコピー・セーフエリア仕様 |
| `07_fcpx-assembly-guide.md` | FCPXタイムライン配置表・グレーディング・書き出し設定 |
| `assets/logo-lockup.svg` | ロゴ（汎用） |
| `assets/ar-scan-overlay.svg` | ARスキャンUIオーバーレイ（透過・1080×1920） |
| `assets/notification-toast.svg` | 成功通知トースト「山崎12年」（透過） |
| `assets/cta-endcard.svg` | CTAエンドカード（1080×1920） |
| `dashboard/index.html` | 制作ダッシュボード（プロンプト/台本コピー・進捗管理・タイムライン可視化） |
| `voice-exports/` | Irodori音声の保存先（命名規則メモ付き） |
| `domoai-exports/` | DomoAI映像の保存先（命名規則メモ付き） |

すべてのSVGは Chromium で 1080×1920 等にラスタライズし、暗背景での表示を目視検証済み。
ダッシュボードもヘッドレスで読み込み、JSエラーなしを確認済み。

---

## 4. 推奨制作手順（新チャットでもこの順）

```
1. 声(Irodori)   ← 04_irodori-voice-script.md の全ラインを生成 → voice-exports/
                    ※先に声を作ると各ショットの実尺が確定し、映像尺を無駄なく設計できる
2. 映像(DomoAI)  ← 03_domoai-prompts.md の keyframe→I2V で SHOT01–09 生成 → domoai-exports/
3. 音(SE/BGM)    ← 05_se-bgm-cue-sheet.md の検索ワードで収集（通知音ピロンが要）
4. 文字/UI       ← assets/ のSVG ＋ 06_graphics-telop-spec.md の字幕
5. 合成(FCPX)    ← 07_fcpx-assembly-guide.md のタイムライン表どおりに並べる
6. グレーディング ← 全DomoAIクリップに統一グレードで質感統一
7. 書き出し      ← H.264 1080×1920 30fps / -14 LUFS で納品
```

---

## 5. 現状ステータス

- [x] 企画・仕様確定（9:16 / 実写シネマティック）
- [x] 全指示書・プロンプト・台本 作成済み
- [x] グラフィック実データSVG 4点 作成・検証済み
- [x] 制作ダッシュボード 作成・検証済み
- [ ] Irodoriで音声生成（制作者作業）
- [ ] DomoAIで映像生成（制作者作業）
- [ ] SE/BGM収集
- [ ] FCPXで合成・グレーディング・書き出し

> 旧AM38リポジトリに一度追加したが、そちらは使わない方針に変更。このパッケージを別リポジトリで運用する。

---

## 6. 新チャットへのキックオフ用プロンプト（コピペ用）

新しいチャットで下記を貼れば、この前提のまま続行できます（`NEW-CHAT-KICKOFF.md` にも同内容）。
